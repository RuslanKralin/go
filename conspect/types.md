# 📚 Основные типы данных в Go

## 🔢 Числовые типы

### Целые числа
- `int` — целое число (32 или 64 бита в зависимости от архитектуры)
  ```go
  var age int = 30
  count := 42  // тип будет выведен как int
  ```
  - *Используется по умолчанию для целых чисел*

- `int8`, `int16`, `int32`, `int64` — целые числа фиксированного размера
  ```go
  var smallNum int8 = 100
  var bigNum int64 = 9_223_372_036_854_775_807
  ```
  - *Полезны для экономии памяти*
  - *Используются при работе с бинарными структурами*

- `uint`, `uint8`, `uint16`, `uint32`, `uint64` — беззнаковые целые (≥ 0)
  ```go
  var positiveNum uint = 100
  var byteVal byte = 255  // byte — псевдоним для uint8
  ```
  - `byte` — псевдоним для `uint8`

### Числа с плавающей точкой
- `float32` — 32-битное число с плавающей точкой
  ```go
  var pi float32 = 3.14159
  ```
  
- `float64` — 64-битное число с плавающей точкой
  ```go
  price := 19.99  // по умолчанию float64
  ```
  - *Рекомендуется для большинства вычислений*

### Комплексные числа
- `complex64`, `complex128` — комплексные числа
  ```go
  var c complex64 = 1 + 2i
  ```
  - *Используются редко, но поддерживаются языком*

## 🔤 Строки и символы

- `string` — строка в кодировке UTF-8
  ```go
  name := "Алексей"
  greeting := "Привет, " + name
  ```
  - *Неизменяемый тип данных*
  - *Любая операция создает новую строку*

- `rune` — символ Unicode (псевдоним для `int32`)
  ```go
  var heart rune = '❤️'  // или 10084
  firstChar := rune("Привет"[0])
  ```
  - *Используется для работы с отдельными символами*

- `byte` — один байт (псевдоним для `uint8`)
  ```go
  var b byte = 65  // 'A' в ASCII
  ```
  - *Применяется при работе с бинарными данными*

## 🧩 Логический тип

- `bool` — логический тип
  ```go
  var isActive bool = true
  isAdmin := false
  ```
  - Допустимые значения: `true` или `false`

## 📦 Составные типы

### Массивы и срезы
- `[n]T` — массив фиксированной длины `n` типа `T`
  ```go
  var numbers [3]int = [3]int{1, 2, 3}
  days := [...]string{"Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"}
  ```
  - *Размер — часть типа*
  - *Используется редко напрямую*

- `[]T` — срез (динамический массив)
  ```go
  nums := []int{1, 2, 3}
  names := make([]string, 0, 10)  // длина 0, ёмкость 10
  ```
  - *Самый популярный контейнер в Go*
  - *Гибкий и удобный в использовании*

### Ассоциативные массивы
- `map[K]V` — словарь «ключ → значение»
  ```go
  user := map[string]interface{}{
      "name": "Алексей",
      "age":  30,
      "city": "Москва",
  }
  ```
  - *Очень быстрый и удобный*
  - *Ключи должны быть сравниваемыми типами*

### Структуры
- `struct` — структура с именованными полями
  ```go
  type Person struct {
      Name    string
      Age     int
      Address string
  }
  
  p := Person{
      Name: "Иван",
      Age:  25,
  }
  ```
  - *Основной способ создавать свои типы данных*
  - *Поддерживает встраивание (embedding)*

### Указатели
- `*T` — указатель на значение типа T
  ```go
  x := 10
  ptr := &x  // ptr имеет тип *int
  *ptr = 20  // изменяем значение по указателю
  ```
  - *В Go нет арифметики указателей, поэтому безопаснее, чем в C*
  - *Используется для передачи по ссылке*

## ⚙️ Специальные типы

### Интерфейсы
- `interface{}` — пустой интерфейс
  ```go
  var anything interface{}
  anything = 42
  anything = "строка"
  anything = []int{1, 2, 3}
  ```
  - *Может содержать любое значение*

- `error` — встроенный интерфейс для ошибок
  ```go
  if err := someFunction(); err != nil {
      log.Fatal(err)
  }
  ```
  - *Стандартный способ обработки ошибок*
  - `type error interface { Error() string }`

### Функции
- `func` — тип функции
  ```go
  // Тип функции, принимающей два int и возвращающей int
  var add func(int, int) int
  
  add = func(a, b int) int {
      return a + b
  }
  ```
  - *Функции — это объекты первого класса*
  - *Могут быть переданы как значения*

### Каналы
- `chan T` — канал для обмена данными между горутинами
  ```go
  messages := make(chan string)
  
  go func() {
      messages <- "Привет из горутины!"
  }()
  
  msg := <-messages
  fmt.Println(msg)
  ```
  - *Сердце конкурентности Go*
  - *Обеспечивает безопасную передачу данных*

## 💡 Советы по использованию типов

- Для большинства числовых операций используйте `int` или `float64`
  ```go
  // Хорошо
  var count int = 100
  var total float64 = 123.45
  ```

- Для работы с символами используйте `rune` вместо `byte`
  ```go
  // Лучше
  firstChar := rune("Привет"[0])  // корректно работает с Unicode
  ```

- Для коллекций предпочитайте срезы (`[]T`) массивам (`[n]T`)
  ```go
  // Предпочтительно
  numbers := []int{1, 2, 3}
  ```

- Используйте `struct` для группировки связанных данных
  ```go
  type Config struct {
      Host    string
      Port    int
      Timeout time.Duration
  }
  ```

- Для обработки ошибок всегда возвращайте `error`
  ```go
  func ReadFile(name string) ([]byte, error) {
      data, err := ioutil.ReadFile(name)
      if err != nil {
          return nil, fmt.Errorf("не удалось прочитать файл: %w", err)
      }
      return data, nil
  }
  ```

- Для конкурентного доступа к данным используйте каналы (`chan`) или мьютексы
  ```go
  var (
      counter int
      mu      sync.Mutex
  )
  
  func increment() {
      mu.Lock()
      counter++
      mu.Unlock()
  }
  ```