# 📚 Конспект по основам Go

## 🔍 1. Типы данных и их вывод

### Определение типа переменной
В Go можно узнать тип любой переменной с помощью форматирования `%T`:

```go
func main() {
    text := "Hello, Go!"
    number := 42
    isActive := true
    
    fmt.Printf("Тип переменной text: %T\n", text)      // string
    fmt.Printf("Тип переменной number: %T\n", number)  // int
    fmt.Printf("Тип переменной isActive: %T\n", isActive) // bool
}
```

**Простыми словами:** `%T` — это специальный символ, который показывает, какого типа данные хранятся в переменной.

---

## ✅ 2. Логические операции и условия

### Сравнение и булевы значения
Результат сравнения можно сохранить в переменную:

```go
func main() {
    var score int = 100
    asd := score == 5  // Проверяем: равен ли score числу 5?
    println(asd)       // false (потому что 100 ≠ 5)
    
    // Другие примеры сравнений
    isGreater := score > 50    // true
    isLess := score < 200      // true
    notEqual := score != 100   // false
}
```

**Простыми словами:** Когда мы сравниваем числа с помощью `==`, `>`, `<`, Go возвращает `true` (правда) или `false` (ложь).

---

## 🔄 3. Циклы (Loops)

### Цикл for с условием
Базовый цикл, который повторяет действия определённое количество раз:

```go
func main() {
    res := rand.Intn(10)  // Случайное число от 0 до 9
    
    for i := 0; i <= res; i++ {
        if i % 2 == 0 {
            fmt.Println("четное", i)
            time.Sleep(500 * time.Millisecond)
        } else {
            fmt.Println("нечетное", i)
            time.Sleep(500 * time.Millisecond)
        }
    }
}
```

**Простыми словами:** 
- `i := 0` — начинаем с 0
- `i <= res` — продолжаем, пока i не превысит res
- `i++` — после каждой итерации увеличиваем i на 1
- `time.Sleep()` — делаем паузу между выводами

### Бесконечный цикл
Цикл без условия работает вечно:

```go
func main() {
    for {
        fmt.Println("Hello")
        time.Sleep(1 * time.Second)
    }
}
```

**Простыми словами:** Этот цикл будет выводить "Hello" каждую секунду, пока программу не остановят вручную (Ctrl+C).

### Цикл с break и continue
```go
func main() {
    for i := 0; i < 10; i++ {
        if i == 5 {
            break  // Прерываем цикл при i == 5
        }
        if i % 2 == 0 {
            continue  // Пропускаем четные числа
        }
        fmt.Println(i)  // Выведет: 1, 3
    }
}
```

---

## ⏱️ 4. Отложенные функции (defer)

### Основы defer
`defer` откладывает выполнение функции до завершения родительской функции:

```go
func main() {
    fmt.Println("Начало")
    
    defer func() {
        fmt.Println("Это выполнится последним")
    }()
    
    fmt.Println("Середина")
    fmt.Println("Конец")
}

// Вывод:
// Начало
// Середина
// Конец
// Это выполнится последним
```

### Множественные defer (стек)
Несколько `defer` выполняются в обратном порядке (LIFO — Last In, First Out):

```go
func main() {
    defer fmt.Println("1")
    defer fmt.Println("2")
    defer fmt.Println("3")
    
    fmt.Println("Основной код")
}

// Вывод:
// Основной код
// 3
// 2
// 1
```

**Простыми словами:** `defer` — это как напоминание "сделай это перед выходом". Если несколько напоминаний, они выполняются в обратном порядке, как стопка тарелок: последняя положенная — первая снимается.

### Практическое применение
```go
func readFile() {
    file, err := os.Open("data.txt")
    if err != nil {
        return
    }
    defer file.Close()  // Файл закроется автоматически при выходе из функции
    
    // Работа с файлом...
}
```

---

## 👉 5. Указатели (Pointers)

### Что такое указатель?
Указатель — это переменная, которая хранит **адрес в памяти** другой переменной.

```go
func main() {
    var num int = 10
    
    pointer := &num              // & — получаем адрес переменной
    fmt.Println(pointer)         // 0xc000106008 (адрес в памяти)
    fmt.Println(*pointer)        // 10 (значение по этому адресу)
    
    *pointer = 20                // Меняем значение через указатель
    fmt.Println(num)             // 20 (исходная переменная изменилась!)
}
```

**Простыми словами:**
- `&num` — "дай мне адрес переменной num"
- `*pointer` — "дай мне значение, которое лежит по этому адресу"

### Указатели в функциях
Передача по указателю позволяет изменять оригинальную переменную:

```go
// Передача по значению (копия)
func incrementCopy(a int) {
    a = a + 1  // Изменяется только копия
}

// Передача по указателю (ссылка)
func incrementPointer(a *int) {
    *a = *a + 1  // Изменяется оригинал
}

func main() {
    num := 10
    
    incrementCopy(num)
    fmt.Println(num)  // 10 (не изменилось)
    
    incrementPointer(&num)
    fmt.Println(num)  // 11 (изменилось!)
}
```

### Зачем нужны указатели?
1. **Экономия памяти** — не создаём копии больших структур
2. **Изменение данных** — функция может менять исходные данные
3. **Производительность** — быстрее передать адрес, чем копировать данные

```go
type User struct {
    Name  string
    Email string
    Age   int
}

// Без указателя — копируется вся структура
func updateAgeCopy(u User, newAge int) {
    u.Age = newAge  // Изменяется только копия
}

// С указателем — передаётся только адрес
func updateAgePointer(u *User, newAge int) {
    u.Age = newAge  // Изменяется оригинал
}

func main() {
    user := User{Name: "Руслан", Email: "test@mail.com", Age: 25}
    
    updateAgeCopy(user, 30)
    fmt.Println(user.Age)  // 25 (не изменилось)
    
    updateAgePointer(&user, 30)
    fmt.Println(user.Age)  // 30 (изменилось!)
}


### Nil в Go

`nil` в Go - это нулевое значение для указателей, интерфейсов, каналов, срезов, отображений и функций. Это специальное значение, которое указывает на отсутствие значения или неинициализированную переменную.

### Где используется nil:

1. **Указатели** - указывает, что указатель ни на что не ссылается
2. **Интерфейсы** - значение по умолчанию для интерфейса
3. **Срезы (slices)** - пустой срез (но не то же самое, что пустой срез с длиной 0)
4. **Отображения (maps)** - неинициализированное отображение
5. **Каналы (channels)** - неинициализированный канал
6. **Функции** - значение по умолчанию для переменной типа функции

### Примеры использования nil:

#### 1. Указатели
```go
var ptr *int        // ptr == nil
if ptr == nil {
    fmt.Println("Указатель не инициализирован")
}
```

#### 2. Интерфейсы
```go
var w io.Writer     // w == nil
if w == nil {
    fmt.Println("Интерфейс не инициализирован")
}
```

#### 3. Срезы, карты и каналы
```go
var s []int         // s == nil
var m map[string]int // m == nil
var c chan int      // c == nil
```

### Важные замечания:
- Проверка на nil - это способ убедиться, что переменная была инициализирована
- Попытка использовать nil-значение (например, разыменовать nil-указатель) вызовет панику
- nil-срез и пустой срез (s := []int{}) ведут себя одинаково при использовании len() и cap()
- nil-карта может быть прочитана, но запись в неё вызовет панику

### Пример с указателями:
```go
func main() {
    number := 10
    var pointer = &number
    fmt.Println(pointer) // 0xc0000120c8 (адрес в памяти)
    
    var pointer2 *int    // по умолчанию nil
    fmt.Println(pointer2) // nil
    
    // Всегда проверяйте указатели на nil перед разыменованием
    if pointer2 != nil {
        fmt.Println("Значение:", *pointer2)
    } else {
        fmt.Println("Указатель не инициализирован (nil)")
    }
}
```

### Когда использовать nil:
1. Для инициализации указателей, когда они еще не ссылаются на данные
2. Для проверки, был ли интерфейс инициализирован
3. Для ленивой инициализации структур (например, синглтонов)
4. Для обозначения отсутствия значения (аналог null в других языках)

### Когда не использовать nil:
1. В качестве возвращаемого значения, если это может вызвать путаницу (лучше возвращать ошибку или специальное значение)
2. Без предварительной проверки перед использованием
3. В ситуациях, когда лучше использовать пустые значения (например, пустой срез вместо nil)
```

**Простыми словами:** Указатель — это как адрес дома. Вместо того чтобы копировать весь дом, мы просто передаём адрес, и любой может прийти по этому адресу и изменить что-то внутри. КОГДА МЫ ПЕРЕДАЕМ В ФУНКЦИЮ УКАЗАТЕЛЬ, МЫ НЕ КОПИРУЕМ ЗНАЧЕНИЕ ПЕРЕМЕННОЙ В ЦЕЛЯХ ЭКОНОМИИ ПАМЯТИ НО МЫ КОПИРУЕМ УКАЗАТЕЛЬ! Хотя при изменении значения через куказатель в других указателях тоже поменяется это значение. Таким образом мы рабоатем с одним и тем же объектом

---

## 📝 Дополнительные примеры

### Объявление переменных
```go
// Полное объявление с типом
var name string = "Руслан"

// Короткая форма (тип определяется автоматически)
age := 25

// Объявление без инициализации (значение по умолчанию)
var count int  // 0
var text string  // ""
var isActive bool  // false
```

### Константы
```go
const Pi = 3.14
const AppName = "MyApp"

// Группа констант
const (
    StatusOK = 200
    StatusNotFound = 404
    StatusError = 500
)
```

---

## 🎯 Полезные советы

1. **Используйте короткую форму `:=`** для локальных переменных
2. **`defer` полезен** для закрытия файлов, соединений, освобождения ресурсов
3. **Указатели нужны** когда хотите изменить данные внутри функции
4. **Избегайте бесконечных циклов** без условия выхода
5. **`%T` для отладки** — быстро узнать тип переменной



## 👉 6. Структуры (Structures)
Структуры — это пользовательские типы данных, которые группируют несколько переменных разного типа.
